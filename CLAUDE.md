# CLAUDE.md — このリポジトリでの作業ルール

このリポジトリは、お弁当記録ブログ「今日の娘弁当」（GitHub Pages で公開）と、
単独アプリ「カロリーノート」が入っています。オーナーは非エンジニアです。

## 大原則

- **このリポジトリは全世界に公開されている。** APIキー・パスワード・トークン・
  個人を特定できる情報（学校名・住所・実名など）は、コード・コメント・コミット
  メッセージのどこにも絶対に入れないこと。
- 説明やコミットメッセージは、**やさしい日本語**で書く（専門用語を避ける）。
- 依頼されていないファイルのリファクタリングや整理はしない。

## ファイル構成と役割

| ファイル | 役割 | ブログ更新で触る？ |
|---|---|---|
| `obento.js` | お弁当の記録リスト（日付・写真・コメント） | ⭕ ここに1件追記する |
| `images/` | お弁当の写真置き場 | ⭕ 写真を追加する |
| `goods.js` / `goods.html` | 愛用グッズ・調味料の紹介ページ | グッズ追加のときだけ |
| `index.html` / `style.css` | ブログの骨組みとデザイン | ❌ 原則触らない |
| `health.*` / `foods.js` / `sw.js` / `manifest.webmanifest` | カロリーノート（独立アプリ） | ❌ ブログ更新では触らない |
| `tools/brighten.py` | 写真の明るさ補正ツール | 写真追加時に使う |

## ふだんの更新手順（「◯月◯日の写真お願い」と言われたら）

1. 写真を `images/YYYY-MM-DD.jpg` の名前で配置する（半角英数字）
2. **明るさ補正**: `python3 tools/brighten.py images/YYYY-MM-DD.jpg` を実行する
   - 方針は「自然に明るく・白飛びさせない」。ツールが自動判断するので設定変更は不要
   - もともと明るい写真はツールが自動でスキップする
3. `obento.js` の `obentoList = [` のすぐ下に1件追記する:
   ```js
   {
     date: "YYYY-MM-DD",
     photo: "images/YYYY-MM-DD.jpg",
     comment: "ひとことコメント",
   },
   ```
4. 写真がない日の日記は `photo: ""` にする。お弁当がなかった日のメモは
   ファイル下部の `oyasumiList` に追加する

## コメントの雰囲気

- ママが娘に向けたやさしい口調。「〜だよ」「〜してみた♡」など
- 絵文字は1〜2個まで（💕 ♡ ✌ 💦 など、過去の投稿に合わせる）
- 1〜2文の短いひとこと。長い説明は書かない

## 作業の進め方

- 変更は `claude/` で始まる作業用ブランチで行い、プッシュ後に PR で確認してもらう
- main へ直接プッシュしない
- コミットメッセージは日本語で「何をしたか」を1行で
